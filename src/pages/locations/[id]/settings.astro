---
import Layout from "../../../layouts/Layout.astro";
import { LocationSettings } from "@/components/locations/LocationSettings";

const { id } = Astro.params;

if (!id) {
  return Astro.redirect("/");
}
---

<Layout title="Ustawienia parkingu">
  <div class="space-y-6">
    <div>
      <h1 class="text-3xl font-bold tracking-tight">Ustawienia parkingu</h1>
      <p class="text-muted-foreground">Zarządzaj ustawieniami i stawkami dla wybranego parkingu.</p>
    </div>

    <LocationSettings client:load locationId={id} />
  </div>
</Layout>
